numeroa = float(input("Ingrese un primer número:"))
numerob = float(input("Ingrese un segundo número: "))

primera = float((numeroa)+(numerob))
segunda = float(numeroa) - float(numerob)
tercera = float(numeroa)*float(numerob)

if opcion == primera:
    print("La suma es " + str(primera))

opcion = str(input("Introduzca la opción que desee mostrar, ya sea primera, segunda o tercera: "))

numeroa = float(input("Ingrese un primer número:"))
numerob = float(input("Ingrese un segundo número: "))

sumar = float((numeroa)+(numerob))
restar = float((numeroa) -(numerob))
multip = float((numeroa)*float(numerob))

if opcion == primera:
    print("La suma es " + str(sumar))
    
opcion = (input("Introduzca la opción que desee mostrar, ya sea primera, segunda o tercera: "))


